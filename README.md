# Face Detection Project

This repository contains a Python face detection project, including the main script `facedetect.py`.

## Open the project directly

Use this direct link to view the project on GitHub:

https://github.com/Tanishq-Mittal/Face-detection-project

If you want to open the project without browsing code, use `WEBSITE_LINK.txt` to access the same URL directly.

## Notes

- This folder has been initialized as a local git repository.
- Use `git push -u origin master` to publish the current commit to GitHub after remote authentication.
